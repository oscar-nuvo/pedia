# Supabase Configuration
# Copy this file to .env and fill in your actual values
# NEVER commit the actual .env file to git

# Supabase Project URL
# Get this from: Supabase Dashboard → Project Settings → API
VITE_SUPABASE_URL=https://pgypyipdmrhrutegapsx.supabase.co

# Supabase Anon Key (PUBLIC - safe to expose, scoped by RLS)
# Get this from: Supabase Dashboard → Project Settings → API
# Important: This should be the NEW key after rotating from the old one
VITE_SUPABASE_ANON_KEY=<your-anon-key-here>

# Optional: Project ID for CLI commands
VITE_SUPABASE_PROJECT_ID=pgypyipdmrhrutegapsx

# Note: OpenAI API key is stored in Supabase Secrets (PediaAIKey)
# It is NEVER stored in .env or exposed to the client
# Configure it in: Supabase Dashboard → Settings → Secrets/Environment Variables
